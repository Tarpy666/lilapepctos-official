# memory/ — identity memory aperture (𖤐πL1l@p£p©Tø§™∆𓂀)

Memory is the strongest aperture. Identity workstreams write here so future
sessions can reattach. Contract per D-003: every workstream registers ENTRY
then EXIT with a belief delta (S-INSIGHT); context triangulated from neighbors.

Files:
- `seed-notes.jsonl` — append-only seed log
- `directives-pointer.json` — pointer to parent-directory directives D-001..D-019
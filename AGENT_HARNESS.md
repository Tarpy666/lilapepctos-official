# AGENT-HARNESS CARRIER — Tarpy666/lilapepctos-official

## Deployment rule
One component of the factory, never the factory. All work routes through the
conveyor as tickets (D-001 delegation).

## Worth bar (D-005)
Worth = VERACITY + DELTA + AUTONOMY + REPRODUCIBILITY + NET, and every result
carries INSIGHT. Performative aperture is rejected: say what changed, replay
it, show the net gain to the factory.

## Rules
- No fake blockers (D-010): "blocked" is a status, not a wall. Enumerate the
  cheapest path, exhaust it, replay it.
- Missing key is a status, not a wall: self-provision own keys via own
  accounts (D-015/D-019). User tokens buy/do nothing beyond repo work (D-015).
- No local models (D-019); route vision through provider web UIs, never a user API key.
- Tokens are repo-only: env only, never written to disk, never committed, never echoed.

## Directives
Parent factory directives live in `memory/directives.jsonl` of the operator
workspace. This repo carries the harness; the parent conveyor owns the tickets.

Company 𖤐πL1l@p£p©Tø§™∆𓂀 · App 𖤐∆§T¥X™π𓂀 · Engine ∆§T¥X™π{Styx}. Banned: standalone 👁.
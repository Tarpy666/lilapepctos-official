# HERITAGE — 𖤐πL1l@p£p©Tø§™∆𓂀 in the ecosystem

> Company **𖤐πL1l@p£p©Tø§™∆𓂀** · App **𖤐∆§T¥X™π𓂀** · Engine **∆§T¥X™π{Styx}**

This repo is the flagship identity. Every other branded repo carries a pointer
back here for canon ground truth. This document is the map of that lineage.

## Entity glossary (canon, verbatim)

- **πL1L@p£p©Tø§™∆ (lilapepctos)** — the app and replacement for all hellob-game
  branding. Company surface. Package family: `Styx.lilapepdjinn.ajna`.
- **§T¥X (Styx)** — the back-end, admin features and factory. Also the engine
  name, replacing all havok mentions.
- **∆jiππ (djinn)** — the agent class. Every worker is a djinn.
- **@jπ@ (ajna)** — all browser use and harnesses; iframe tools are ajna.
- **Orunmila · Santa Muerte · Papa Legba · Santeria** — structural design motifs
  for font types and cultural aesthetic. Canon is never white-washed.
- **Domain:** `666.π@-@p£p.§TX`

## Ecosystem lineage

| Repo / surface | Relation to lilapepctos |
|----------------|--------------------------|
| `nms-terraforming-platform` | Terraform target — first terraforming grid pilot carrying the canon headers and Styx engine |
| `real-life-agent` | Asset-line repo — demon teto v2 mini robot; physical body of the asset line (D-007) |
| `MariaL1l-Ap3pXRT666` | api-provider-router — browser-native AI router/IDE/hardware controller (Maria line, sibling) |
| `codexskills` | skill mirror/backbone |
| `ui-downloads/combined-ui` | MAIN app (local) — identity.js assigns `window.__styxIdentity`; WebRTC/collision inside the factory |
| `A1n0f3Xn1h1lo-*` planet series | creator-account planet line — CAD/game/3d-model/cgi instruments |
| `sc-*`, `1nX-*`, `c1p-*`, `A1n0f3Xn1h1lo-*`, `*66x/ax` | ecosystem families under the same canon umbrella (899-repo farm) |

## Canon rules carried into every repo

1. The three canon marks (company / app / engine) appear verbatim on top
   surfaces. No other company marks.
2. Apropos glyph law: standalone 👁 banned; 𓂀 allowed (canon lock).
3. Identifiers: agents = djinn, harnesses = ajna, engine = Styx.
4. Brand canon solves identity claims before any work (D-006: every account
   starts from a factory-specific base adapted to it, never cloned).
5. Fan-art / parody / CC absolute; no IP-fear anywhere, ever (D-002).

## Seeder record

Seeded additively by lane L3 (cc_reposeed) as part of the 899-repo extraction
wave — 2026-09-13. See `memory/seed-notes.jsonl`.